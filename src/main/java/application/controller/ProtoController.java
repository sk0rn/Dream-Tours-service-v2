package application.controller;

import application.domain.Duration;
import application.domain.Place;
import application.domain.Subject;

import java.util.HashSet;
import java.util.Set;

public class ProtoController {
    private final static Set<Subject> subjects = new HashSet<>();
    private final static Set<Place> places = new HashSet<>();
    private final static Set<Duration> durations = new HashSet<>();
}
